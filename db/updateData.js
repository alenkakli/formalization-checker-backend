const db = require('./db');

const updateExercise = (data) => {};

const updateProposition = (data) => {};

const updateFormalization = (data) => {};

module.exports = {
  updateExercise,
  updateProposition,
  updateFormalization
};
