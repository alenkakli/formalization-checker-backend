const db = require('./db');

const selectExercise = (exercise_id) => {};

const selectProposition = (proposition_id) => {};

const selectFormalization = (formalization_id) => {};

module.exports = {
  selectExercise,
  selectProposition,
  selectFormalization
};
