const db = require('./db');

const deleteExercise = (data) => {};

const deleteProposition = (data) => {};

const deleteFormalization = (data) => {};

module.exports = {
  deleteExercise,
  deleteProposition,
  deleteFormalization
};
